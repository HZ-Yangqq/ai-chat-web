import type { CustomInterrupt } from '../../core/types'

export interface CustomInterruptRendererProps {
  interrupts: CustomInterrupt[]
}

/** 占位组件：Phase 4 实现自定义打断渲染。 */
export default function CustomInterruptRenderer(_props: CustomInterruptRendererProps) {
  return null
}
